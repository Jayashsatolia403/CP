#include <bits/stdc++.h>

using namespace std;

#define forn(i, n) for (long i = 0; i < n; i++)

#define ll long long



int main() {
    int t;
    cin >> t;

    while (t--) {
        int n;
        cin >> n;

        int arr[n];

        forn (i,n) {
            cin >> arr[i];
        }

        
    }
}