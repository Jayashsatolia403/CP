#include <bits/stdc++.h>

using namespace std;

#define forn(i, n) for (int i=0; i < n; i++)

#define ll long long



int main() {
    int N, U;
    cin >> N >> U;

    forn (i, U) {
        int L, R;
        cin >> L >> R;
    }
}