#include <bits/stdc++.h>

using namespace std;

#define forn(i, n) for (int i=0; i < n; i++)
#define ll long long 



int main() {
    int t;
    cin >> t;

    while (t--) {
        int n, k;
        cin >> n >> k;

        vector<int> p(n,0);

        forn (i, n) {
            cin >> p[i];
        }

        
    }
}