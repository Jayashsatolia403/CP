#include <bits/stdc++.h>
using namespace std;




int main() {
    int n;
    cin >> n;

    vector<int> times(n), directions(n);

    for (int i=0; i < n; i++) cin >> times[i];
    for (int i=0; i < n; i++) cin >> directions[i];

     
}